# Moved — build from `docs/powerbi_guide.md`

**Do not build from this file.** The single build document is
**[`../docs/powerbi_guide.md`](../docs/powerbi_guide.md)** — spec, click-path,
panel layout, prohibition pass, build order, and export steps in one place.

`docs/deneb-specs/` holds the three Deneb chart specs the guide references
for Panels 2A, 2B, 2C, and 3B (`04`–`06`).

Still in this folder: `HINTS7-Commitment-Scorecard.pbip` (the working file —
open this directly in Power BI Desktop), the `.SemanticModel/` / `.Report/`
project definition, the exported `HINTS7-Commitment-Scorecard.pdf`,
`panelB_deneb_spec.json`, and `manual-fallback/`. The guide points at each by
path.
